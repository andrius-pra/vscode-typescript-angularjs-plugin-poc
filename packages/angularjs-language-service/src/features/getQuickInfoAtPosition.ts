import { Context } from '../types/context';

export function getQuickInfoAtPosition(
    context: Context,
    fileName: string,
    position: number
): ts.QuickInfo | undefined {

    return undefined;
}